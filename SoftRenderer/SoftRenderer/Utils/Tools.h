#ifndef  __TOOLS__H__
#define  __TOOLS__H__

#include <stdio.h>
#include<stdlib.h>
#include<string.h>
#include "Constants.h"
#include "DataType.h"
#include "Utils.h"

#ifndef max
#define max(a, b) ((a) > (b) ? (a) : (b))
#endif

#ifndef min
#define min(a, b) ((a) < (b) ? (a) : (b))
#endif
#endif